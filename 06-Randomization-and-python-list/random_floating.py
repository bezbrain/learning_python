import random

print(random.random())  # This is to generate floating numbers from 0 but less than 1
print(
    random.uniform(1, 5)
)  # Generate random floating numbers between 1 and 5, 1 and 5 inclusive
